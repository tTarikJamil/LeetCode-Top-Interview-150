class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        dic = {}
        for i in strs:
            words = "".join(sorted(i))
            if words in dic:    
                dic[words].append(i)
            else:
                dic[words] = [i]
        return list(dic.values())
            